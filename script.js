


/* function logUserDecision(screenNumber) {
    // En esta función, puedes enviar la información de la decisión del usuario a un servidor Python
    // o realizar cualquier otra acción necesaria para registrar los datos.
    // Por ejemplo, puedes usar AJAX para enviar datos al servidor.
    // Aquí se muestra un ejemplo ficticio:
    fetch('/api/log_decision', {
        method: 'POST',
        body: JSON.stringify({ screenNumber }),
        headers: {
            'Content-Type': 'application/json'
        }
    })
    .then(response => response.json())
    .then(data => {
        console.log('Registro exitoso:', data);
    })
    .catch(error => {
        console.error('Error al registrar la decisión:', error);
    });
}

// Mostrar la primera pantalla al cargar la página
navigateToScreen(1); */